services:
  app:
    image: ghcr.io/nghiahoang/truyenqq-clone:latest
    env_file:
      - .env
    restart: unless-stopped

  tunnel:
    image: cloudflare/cloudflared:latest
    command: tunnel run --token ${CLOUDFLARED_TOKEN}
    restart: unless-stopped
